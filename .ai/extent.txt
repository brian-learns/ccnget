the current working directory contains the code for ccnget

```bash
$ uv run ccnget config show
Config file: /home/hermes/.config/ccnget/config.json

  cdx-url = http://0.0.0.0:7860/lookup
    source: config
  cc-crawl-base-url = https://data.commoncrawl.org
    source: env (CC_CRAWL_BASE_URL)
```

The server at http://0.0.0.0:7860/ had just added a new endpoint /extent

The task is to add a way to query the endpoint to the cli, maybe

```
$uv run ccnget extent
```

from http://0.0.0.0:7860/openai.json

    "/extent": {
      "get": {
        "summary": "Extent Endpoint",
        "description": "show what content is indexed on this server",
        "operationId": "extent_endpoint_extent_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ExtentResponse"

      "ExtentResponse": {
        "properties": {
          "file_extent": {
            "type": "integer",
            "title": "File Extent",
            "description": "number of files covered by this index"
          },
          "file_oldest": {
            "type": "string",
            "title": "File Oldest",
            "description": "first WARC file in this index"
          },
          "file_newest": {
            "type": "string",
            "title": "File Newest",
            "description": "last WARC file added to this index"
          }
        },
        "type": "object",
        "required": [
          "file_extent",
          "file_oldest",
          "file_newest"
        ],
        "title": "ExtentResponse",
        "description": "Extent of the WARC files in this index"
      },
