I wanted to get some more URLs to do testing with

`random_sample.py` shows how to query the parquet files in the hugging face dataset

To save time, the samples in this directory were taken from only 14 of the 119 files

The CSV files have these headers

 * Surt
 * capture_time
 * url
 * mime
 * status
 * digest
 * length
 * offset
 * filename
 * month
 * year
